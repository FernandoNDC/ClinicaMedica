@model IEnumerable<ClinicaMedica.Models.Entidades.RemedioPrescrito>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Medico.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Paciente.Nome)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Remedio.NomeGenerico)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Posologia)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Medico.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Paciente.Nome)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Remedio.NomeGenerico)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Posologia)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RemedioPrescritoId }) |
            @Html.ActionLink("Details", "Details", new { id=item.RemedioPrescritoId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RemedioPrescritoId })
        </td>
    </tr>
}

</table>
